spring:
  application:
    name: bai2
  datasource:
    url: jdbc:mysql://localhost:3333/auth_jwt?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: MY_SUPER_SECRET_KEY_123456789012345678901234567890
  access-expiration: 900000 # 15 ph�t
  refresh-expiration: 2592000000 # 30 ng�y
server:
  port: 8888
