version: '3.9'
services:
  auth-jwt:
    image: mysql:8.3.0
    container_name: auth_jwt
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: auth_jwt
    ports:
      - "3333:3306"
    volumes:
      - auth_data:/var/lib/mysql
    restart: always

volumes:
  auth_data:
